# .env 檔案：存放環境變數，不應提交到版本控制 (已加入 .dockerignore 和 .gitignore)

# -----------------------------------------------------------------------------
# 資料庫連線設定
# 預設使用 SQLite，資料庫檔案 'warehouse.db' 將放在專案根目錄的 'data/' 目錄下。
# -----------------------------------------------------------------------------
DATABASE_URL="sqlite:///./data/warehouse.db"

# -----------------------------------------------------------------------------
# PostgreSQL 資料庫連線範例 (如果使用 Docker Compose 部署 PostgreSQL)
# 請取消下方註解並根據您的實際配置進行修改。
# -----------------------------------------------------------------------------
# DATABASE_URL="postgresql://user:password@db:5432/warehouse_db"
# POSTGRES_USER=user
# POSTGRES_PASSWORD=password
# POSTGRES_DB=warehouse_db
# DB_HOST=db # Docker Compose 中的服務名稱

# 是否在應用程式啟動時自動創建資料庫表格 (僅限開發環境/測試，生產環境應使用 Alembic)
# 設為 "true" 則自動創建，"false" 則不創建。生產環境強烈建議設為 "false"
CREATE_TABLES_ON_STARTUP="false"

# 控制 SQLAlchemy/SQLModel 是否在控制台打印所有執行的 SQL 語句。
# 設為 "true" 則啟用，"false" 則關閉。
SQL_ECHO="false"

# 其他範例環境變數
EXAMPLE_VAR="Hello From .env! This is a test variable."
APP_SECRET_KEY="your_secret_key_here_for_security_purposes_change_this" # 應用程式的秘密金鑰 (例如用於 JWT)，務必更改！
LOG_LEVEL="INFO" # 日誌級別 (INFO, DEBUG, WARNING, ERROR)
